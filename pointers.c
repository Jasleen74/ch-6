#include<stdio.h>
int main() {
     
     int b = 112;
     printf("The address of the a is %p\n",&b); // we will always use this
     printf("The address of a in integer is %u",&b); // unsigned integer
  
 return 0;
}